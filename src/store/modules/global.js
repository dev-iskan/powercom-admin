/* eslint-disable no-shadow */

const state = {
  loading: false,
};

const getters = {

};

const actions = {

};

const mutations = {
  setLoading(state, loading) {
    state.loading = loading;
  },
};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
